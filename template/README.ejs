🤩 我的star列表，0,8,16自动更新 🤩
<div>
<img src="https://camo.githubusercontent.com/82291b0fe831bfc6781e07fc5090cbd0a8b912bb8b8d4fec0696c881834f81ac/68747470733a2f2f70726f626f742e6d656469612f394575424971676170492e676966" width="800"  height="3">
</div><br>

## Table of Contents

<% for (let [language, repositories] of stars) { %>
## <%= language %>
  <% 
    // 对每种语言下的仓库进行排序
    repositories.sort((a, b) => b.stargazers_count - a.stargazers_count);
    // 只取前 100 个仓库
    const topRepositories = repositories.slice(0, 100);
  %>
  <% for (let repo of topRepositories) { %>
- [<%= repo.full_name %>](<%= repo.html_url %>) - <%= repo.description ? repo.description.slice(0, 100) : '' %>
  <% } %>
<% } %>
