<div align="center">

# my-stars

🤩 我的 Star 列表，每天凌晨自动更新 🤩

</div><br>

## 目录

1. [语言分类](#语言分类)
2. [贡献](#贡献)
3. [许可证](#许可证)

## 语言分类

<% 
  const languages = Object.keys(stars); 
  // Initialize an 'Other' category if there are any repositories that don't fall into known languages
  if (!languages.includes('Other')) {
    languages.push('Other');
  }
  
  for(let language of languages) { 
    const repositories = stars[language] || []; // Default to empty array for 'Other'
%>
### <%= language %>
  <% for(let repo of repositories) { %>
- [<%= repo.full_name %>](<%= repo.html_url %>) - <%= repo.description %>
  <% } %>
  <% if (repositories.length === 0 && language === 'Other') { %>
  No repositories found in this category.
  <% } %>
<% } %>

## 贡献

欢迎提出建议和贡献代码！请查看 [贡献指南](CONTRIBUTING.md)。

## 许可证

本项目采用 [MIT 许可证](LICENSE)。
